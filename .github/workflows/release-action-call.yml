name: Release action
# Call the reusable workflow release-solution-with-inputs.yml
# Release your solution to prod when you create a new release.

on:
  release:
      types: [created]        
permissions:
  contents: write
jobs:
  Release-solution-ALMLab:
    uses: ./.github/workflows/release-solution-to-prod-with-inputs.yml
    with:
      #You can specify the solution name here
      solution_name: MicrosoftxGitHubRoadShowSample
      #Update your values here
      BUILD_ENVIRONMENT_URL: https://xxxbuildaugusxxxt.crm.dynamics.com
      PRODUCTION_ENVIRONMENT_URL: https://orgd678701b.crm5.dynamics.com
      CLIENT_ID: 22
      TENANT_ID:   22
    secrets:
      envSecret: ${{ secrets.PowerPlatformSPN2 }}
